N,M = map(int,input().split())
l = list(map(int,input().split()))
l1 = []
for a in range(len(l)):
    for b in range(a+1,len(l)):
        for c in range(b+1,len(l)):
            if l[a]+l[b]+l[c] <= M:
                l1.append(l[a]+l[b]+l[c])
print(max(l1))

포문을 요번처럼 많이 겹쳐본적은 없던것같다

브루트포스는 전수조사라는 뜻이고 말그대로 모든 결과를 전부 알아내야 한다
5장을 뽑으면 5장 안에서 나올 수 있는 모든 결과를 도출해야 하는 터미네이터1에 나올법한 수법이다
